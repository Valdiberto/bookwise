import { styled } from '../../../../stitches.config'

export const Container = styled('div', {
  display: 'flex',
  gap: '$5',
  alignItems: 'center',
  svg: {
    color: '$green100',
    width: 32,
    height: 32,
  },
})

import { styled } from '../../../../stitches.config'

export const BookInfoContainer = styled('div', {
  display: 'flex',
  alignItems: 'center',
  gap: '$4',

  svg: {
    color: '$green100',
    width: 24,
    height: 24,
  },
})
